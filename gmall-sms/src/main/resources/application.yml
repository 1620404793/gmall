server:
  port: 8085
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/guli_sms?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: 123456
#使用mybatis-plus
mybatis-plus:
  global-config:
    db-config:
      id-type: auto  #默认使用数据库自增的方式
  mapper-locations: classpath:mapper/sms/**/*.xml  #读取mapper映射文件